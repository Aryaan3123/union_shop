class MockCartProvider extends ChangeNotifier implements CartProvider {
  // Override methods and properties as needed for tests
}

class MockAuthProvider extends ChangeNotifier implements AuthProvider {
  // Override methods and properties as needed for tests
}
